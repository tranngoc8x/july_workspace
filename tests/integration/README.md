# Integration Tests

This directory is reserved for tests that verify contracts across the July
Workspace modules, especially application use cases, transport ports, and
SQLite storage. Phase 1's concrete persistence integration coverage currently
lives in `tests/core_sqlite.rs`; add broader tests here when later-phase module
boundaries exist.
